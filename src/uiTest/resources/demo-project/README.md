# Bluloco screenshot project

This tiny project is opened by `runIdeForUiTests`. It intentionally contains a partially typed Java call so the Robot suite can show and assert the real IntelliJ completion popup.
